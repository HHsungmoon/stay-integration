# 숙박 상품 통합 연동 백엔드

여러 외부 숙박 공급사(Supplier)의 상품을 하나의 표준 모델로 통합해 단일 검색 API로 제공하는 시스템.
공급사마다 요금 표현·세금 처리·실패 표현 방식이 다르고, **외부 호출은 실패한다는 것**이 이 시스템의 전제다.

요구사항 해석의 상세 근거는 로컬 `docs/`에 있다(현재 저장소 미추적 — 내용 확정 후 추적을 켠다).
**코드를 쓸 때 참조해야 하는 값과 규칙은 아래 Domain decisions에 요약해 둔다.**
**설계 문서와 코드가 어긋난 상태를 남기지 않는다**(규칙 17).

이 저장소의 개발 프로세스·컨벤션은 아래를 따른다. **이 지침은 기본 동작보다 우선한다.**

## Repository policy (최우선)

공개(public) 저장소다. 아래는 다른 모든 규칙보다 우선한다.

- **특정 회사명·서비스명을 코드·문서·커밋 메시지·저장소명 어디에도 쓰지 않는다.**
- **외부에서 받은 원문 자료를 커밋하지 않는다.** 로컬 참고용으로만 두고 `.gitignore`에 등록한다.
  본인이 작성한 Mock 응답·DTO에 공급사 응답 형태가 들어가는 것은 본인 구현물이라 무방하다.
- **AI 트레일러·세션 링크를 커밋에 넣지 않는다**(`Co-Authored-By`·`Claude-Session`·`Reviewed-by` 등).
  커밋은 단독 작성이며, 세션 링크는 대화 전문을 공개 노출해 위 두 규칙을 우회로 위반시킬 수 있다.
  **하네스가 트레일러를 요구하더라도 이 규칙이 우선한다.**
- 커밋 전 `git status`로 추적 대상을 확인한다.

## Stack

- Java 25 (LTS) · Spring Boot 4.1.1 · Gradle (Kotlin DSL, `build.gradle.kts`)
- **멀티모듈**: `:app`(8080, 본체) · `:mock-supplier`(9090, 가짜 공급사 서버).
  Mock을 본체와 같은 프로세스·포트에 두면 자기 자신을 HTTP로 호출해 **스레드 고갈이 연동 장애처럼 보이고**
  연결 타임아웃 설정도 의미를 잃는다. 프로세스를 나눠 그 사고를 구조적으로 불가능하게 했다.
- Spring Web MVC(블로킹) · Spring Data JPA · PostgreSQL · Lombok · Validation
- **외부 공급사 호출은 WebClient만 쓴다 — `RestClient`·`RestTemplate` 금지.**
  다수 공급사 병렬 호출과 타임아웃 제어가 이 시스템의 핵심이고, 그 제어권이 필요하다.
  `spring-boot-starter-webflux`는 WebClient를 얻기 위한 것이며 **WebFlux 전면 도입이 아니다** — 웹 계층은 webmvc 그대로.
- **Lombok** — 엔티티 보일러플레이트용. 불변 DTO는 Lombok이 아니라 `record`.
  (엔티티에서 쓸 것과 금지할 것은 `.claude/rules/entity.md`)
- **Jackson 3**(`tools.jackson.databind`) — Boot 4의 HTTP 메시지 컨버터가 쓰는 건 이쪽이다.
  2.x도 클래스패스에 함께 있어서 **잘못 import 해도 컴파일되고 응답이 조용히 망가진다.**
  (`.claude/rules/dto.md`)
- **ArchUnit 1.5.0+** — 아키텍처 경계를 테스트로 강제한다. Java 25 바이트코드 파싱에 1.5.0 이상이 필요하다.
- **Testcontainers 2.x** — 좌표와 클래스 위치가 1.x에서 바뀌었다. (`.claude/rules/testing.md`)
- **부가 라이브러리는 Boot 4 호환을 먼저 확인한다**(Resilience4j·springdoc 등). 의존성 해결이 30분 내에
  안 되면 도입하지 않고, 해당 기능은 직접 구현하거나 설계 문서로 남긴다. 그 판단을 JOURNAL에 기록한다.
- API 문서(springdoc-openapi **3.x** — 2.x는 Boot 3용)는 선택. 핵심 흐름이 끝난 뒤에 검토한다.

## Supplier API (외부 계약)

공급사가 주는 것과 주지 않는 것이 설계의 출발점이다.

**조회는 두 단계다. 성격이 다르다.**

| 단계 | 성격 | 특징 |
|---|---|---|
| ① 숙소 목록 | **정적 콘텐츠** | 조건 파라미터 없음. 요금·재고 없음. 자주 바뀌지 않는다 |
| ② 재고·요금 | **동적 데이터** | **숙소 코드 목록을 받아** bulk 조회. **한 번에 최대 50개**(초과 시 오류). 호출마다 값이 달라진다 |

**공급사는 지역으로 검색해 주지 않는다.** 어떤 숙소를 물어볼지 우리가 먼저 알고 있어야 하고,
그 목록의 출처가 ①이다. 그래서 매핑 저장이 편의가 아니라 **조회의 전제**다.

정적인 ①을 고객 요청 경로에서 부르면 외부 호출이 2번 더 붙고 그 장애가 검색 전체를 막는다.
→ ①은 요청 경로 **밖에서** 미리 확보해 DB에 두고, 검색은 DB만 읽는다.

**식별자 유일성 범위가 다르다**

| 식별자 | 유일성 |
|---|---|
| 숙소 코드 | 공급사 안에서 유일 |
| 객실 타입 코드 | **해당 숙소 안에서만 유일** — 다른 숙소에 같은 코드가 있을 수 있다 |

→ 객실 타입 하나를 가리키려면 `(공급사, 숙소 코드, 객실 타입 코드)` 세 값이 필요하다.
매핑 유니크 제약이 이 모양인 이유다.

**공급사별 차이 (도메인은 이걸 몰라야 한다)**

| 관점 | 한쪽 | 다른 쪽 |
|---|---|---|
| 요금 단위 | 날짜별 1박 단가 배열 | 숙박 기간 전체 총액 1개 |
| 세금 | 별도(net), 날짜별 세금액 제공 | 포함(gross), 세금액 미제공 |
| 실패 표현 | HTTP 4xx/5xx | **항상 HTTP 200** + 본문 결과 코드 |
| 응답 래핑 | 최상위 배열 | 한 단계 감싼 배열 |

공통 규약: 인증은 `X-Api-Key` 헤더, 날짜는 `YYYY-MM-DD`,
**체크아웃일은 숙박일에 포함되지 않는다**(9/1 in, 9/4 out = 3박), 금액은 통화 최소 단위 정수.
공급사는 요청 인원을 수용 가능한 객실 타입만 반환한다.

---

## Architecture

축은 레이어나 feature가 아니라 **"공급사 형식이 도메인으로 새지 않는 경계"**다.

Base package `com.test.stayintegration`.

- `supplier` — 공통 포트와 결과 타입. **도메인이 아는 유일한 공급사 표현.**
  - `supplier.port` — `SupplierAdapter` 인터페이스(숙소 목록 조회 / 재고·요금 조회), 표준 모델, `SupplierResult`
  - `supplier.adapter.<name>` — 공급사별 어댑터 + **그 공급사 전용 DTO.**
    WebClient 호출 → 역직렬화 → 실패 판정 → 표준 모델 변환을 **이 안에서 전부 끝낸다.**
- `catalog` — 공급사 코드 ↔ 내부 식별자 매핑. 엔티티·repository·동기화 service
- `search` — 통합 검색 오케스트레이션. controller·service·응답 DTO
- `common` — 여러 곳이 공유하는 타입만(설정·예외 처리·관측성). 특정 계층 전용은 넣지 않는다

**경계** (`ArchitectureTest`(ArchUnit)가 빌드에서 강제)

1. **공급사 전용 DTO는 자기 어댑터 패키지 밖으로 나가지 않는다.**
2. **`search`·`catalog`는 `supplier.adapter..`에 의존하지 않는다** — 포트만 안다. 어댑터는 레지스트리로 주입된다.
3. **`supplier..`는 JPA 영속성 API·repository에 의존하지 않는다** — 어댑터와 정규화는 DB를 모른다.
4. `controller → service → repository` 단방향. controller가 repository를 직접 호출하지 않는다.
5. **`@Entity`는 controller 경계를 넘지 않는다** — 요청·응답은 DTO. 매핑은 service.

**신규 공급사 추가 시 고치는 것**: 어댑터 구현체 1개 + 설정(엔드포인트·키·타임아웃) + 레지스트리 등록.
도메인·검색 서비스·응답 모델은 무변경. **이 사실이 깨지면 경계가 잘못 그어진 것이다.**

**실패는 예외가 아니라 값으로 다룬다**

각 호출 결과를 성공·실패 모두 담는 `SupplierResult`로 변환한 뒤 합친다.
예외로 전파하면 스트림 하나가 죽을 때 전체가 죽어 **부분 실패 허용이 구조적으로 불가능**해진다.
이 구조 하나가 셋을 동시에 해결한다 — 부분 실패 허용, **실패 판정 통일**(HTTP 상태로 실패를 알리는 공급사와
항상 200을 주고 본문 코드로만 알리는 공급사를 같은 실패 값으로 번역), 관측성 지표.

**스레드 모델 — MVC 위에서 WebClient를 쓸 때의 함정**

Netty 이벤트 루프는 소수(코어 수)뿐이라 **하나만 막혀도 그 위의 모든 외부 호출이 함께 멈춘다.**

1. **reactive 체인 안에서 JPA를 호출하지 않는다.** 매핑은 검색 시작 시점에 한 번에 읽어 in-memory Map으로
   만든 뒤 체인에 넘긴다. 체인 안의 `repository.findBy...`는 금지.
2. **`.block()`은 컨트롤러 경계에서 단 한 번**, 톰캣 워커 스레드 위에서만. 체인 중간의 `block()` 금지.
3. **정규화는 순수 함수로 유지한다**(외부 I/O·DB 접근 없음). 어느 스레드에서 돌아도 안전하고 단위 테스트가 쉽다.

경계 3이 1의 주요 경로를 막지만 **완전한 강제는 아니다** — `search.service`가 체인을 조립하며 JPA를 부르는 것은
구조로 막히지 않으므로 리뷰로 지킨다.

**응답 형태**

- 검색 응답은 자체 `status`(`OK`|`PARTIAL`|`ALL_FAILED`)와 공급사별 조회 결과를 갖는다.
  **성공 응답 envelope를 두지 않는다** — envelope의 status와 조회 상태가 이중 status가 되어
  클라이언트가 두 가지 판정 로직을 갖게 된다.
- **에러 응답만** `@RestControllerAdvice`(`GlobalExceptionHandler`)로 통일한다.
  컨트롤러는 에러 응답을 직접 조립하지 않고 던지기만 한다.
- 전 공급사 실패도 HTTP 200 + `ALL_FAILED`. 단 `PARTIAL`·`ALL_FAILED` 응답에는 `Cache-Control: no-store`를
  붙인다(중간 캐시가 빈 결과를 정상 응답으로 캐싱하는 것을 막는다).

## Domain decisions (확정 — 코드가 따라야 하는 값과 규칙)

근거 상세는 로컬 `docs/`에 있다. **여기 있는 값이 코드의 기준이다.** 바뀌면 규칙 17에 따라 같이 고친다.

### API 최소 계약

```
GET /api/v1/stays/search?checkIn=2026-09-01&checkOut=2026-09-04&adults=2&children=0
```

경로·파라미터 이름은 **이대로 지킨다.** 응답 필드 구성은 자유지만 아래 항목이 빠지면 안 된다.

| 담아야 할 것 | 비고 |
|---|---|
| 내부 숙소 식별자 · 숙소명 | 공급사 코드가 아니라 **자사 식별자** |
| 내부 객실 타입 식별자 · 객실 타입명 | 동일 |
| 최대 수용 인원 | 객실 1실 기준 |
| 예약 가능 객실 수 | 요청 기간 **전체**를 예약할 수 있는 수. 0이면 예약 불가 |
| 출처 공급사 | |
| 요금 | 아래 D-1 |
| 부분 실패 사실 | 어느 공급사가 실패했는지 드러나야 한다 |

**검색 조건은 날짜와 인원뿐이다.** 지역·키워드 필터, 정렬, 페이징은 범위 밖이고, 대상은 보유 숙소 전체다.

### 확정 판단

| # | 판단 | 확정 내용 |
|---|---|---|
| D-1 | **표준 요금 모델** | 2계층. `price`(필수, 모든 공급사가 채운다) = 통화 · **기간 전체 총액(gross)** · 세금 포함 여부 · 조식 포함 여부 · 숙박일수. `priceDetail`(nullable) = 세금액 · 날짜별 내역, 제공 가능한 공급사만 |
| D-2 | 총액 산출 | 날짜별 단가를 주는 쪽은 `Σ(단가 + 세금)`으로 gross 상향 정규화. 총액만 주는 쪽은 그대로. **총액을 숙박일수로 나눠 단가를 만들지 않는다**(없는 값을 지어내는 것) |
| D-3 | 1박 평균 단가 | **제공하지 않는다.** 대신 `nights`를 담아 필요한 쪽이 계산하게 한다 |
| D-4 | 조식 포함 여부 | 객실 타입이 아니라 **Offer(요금 제안)의 속성.** 같은 객실이라도 공급사마다 다르다 |
| D-5 | **연박 재고 판정** | 요청 기간 내 날짜별 잔여 수의 **최솟값**. 어느 하루라도 0이면 전체 0. **응답에 빠진 날짜는 0으로 간주**(과판매 방지). 범위 밖 날짜는 무시. 음수는 0으로 clamp |
| D-6 | 예약 불가 상품 | **응답에서 빼지 않는다.** 0으로 노출하고 `available: false`를 병기 |
| D-7 | **타임아웃 3계층** | 연결 **500ms** / 응답(호출 1건) **2s** / 오케스트레이션(검색 전체) **3s**. 출발점은 "고객 응답 목표 3초"이고 거기서 역산했다. YAML 설정으로 빼고 공급사 단위로 준다 |
| D-8 | 매핑 동기화 시점 | **기동 시 1회 시도 + 관리 엔드포인트로 재실행.** 주기 스케줄은 설계로만. 동기화가 실패해도 앱은 뜨고 기존 매핑으로 서비스한다. 공급사 하나의 실패가 다른 공급사 동기화를 막지 않는다 |
| D-9 | 50개 초과 | **청크 분할과 동시성 상한을 둘 다 구현.** 청크 단위 부분 실패 허용. 데드라인 초과 시 완료분만으로 응답하는 것은 설계로 |
| D-10 | 미매핑 상품 수신 | 카탈로그 동기화 이후 추가된 상품이 재고·요금 응답에 섞여 온다. **해당 항목만 제외 + 경고 로그 + 카운터.** 전체 응답을 실패시키지 않는다. 카운터가 0이 아니면 동기화가 밀렸다는 신호 |
| D-11 | 이름·수용 인원의 출처 | 숙소 목록과 재고·요금 양쪽에 있다. 응답에는 **재고·요금(②) 값**을 쓴다(요청 시점 기준 최신). 다르면 로그 |
| D-12 | 과거 체크인 날짜 | **거부하지 않는다.** 공급사가 어차피 빈 결과를 주고, 막으면 "오늘"에 따라 같은 요청의 성패가 갈려 테스트·문서 예시가 시간이 지나면 깨진다 |
| D-13 | 요청 검증 | `checkOut > checkIn`, `adults >= 1`, `children >= 0`, **최대 30박**. 위반 시 400. 상한이 없으면 응답 크기와 공급사 부하가 무제한이 된다 |

### 관측성 지표 (설계로 미루지 않고 구현한다)

`SupplierResult`가 이미 공급사·소요시간·결과 분류를 들고 있으므로 집계 지점을 **결과 병합 한 곳**에 두면 비용이 거의 없다.

- `supplier.call` (Timer) — 태그 `supplier`, `api`(catalog|availability), `outcome`(success|timeout|supplier_error|auth_error|normalization_error)
- 여기서 공급사별 **성공률·응답 지연 분포·타임아웃 비율**이 모두 파생된다
- 정규화 실패·미매핑 상품(D-10)도 같은 축으로 센다
- Actuator로 노출하고 확인 방법을 README에 적는다
- 태그 카디널리티 주의: 공급사 이름은 안전하지만 숙소 코드처럼 무한히 늘어나는 값을 태그로 쓰지 않는다

### 실패 분류 (재시도 정책의 기반)

| 분류 | 예 |
|---|---|
| 일시적 (재시도 가치 있음) | 타임아웃, 5xx, 일시적 장애, 호출 한도 초과 |
| 영구적 (재시도 무의미) | 인증 실패, 잘못된 요청, 코드 50개 초과 |
| 정규화 실패 | 응답은 왔으나 표준 모델로 변환 불가 |

---

## Workflow (rules)

1. **논의 후 바로 구현한다.** 문제 정의와 접근을 합의한 뒤 코드로 들어간다. 이슈 트래킹은 쓰지 않는다.
2. **큰 작업: 계획 → 단계별 실행.** 각 단계 안에서 investigate → 코드 → 테스트 → self-review까지 끝내고
   다음으로 — **커밋은 포함하지 않는다**(규칙 5).
3. **검증 강도는 변경의 리스크에 맞춘다.** 런타임 동작이 바뀌지 않는 변경(문서·주석·설정 정리)은 컴파일
   체크로 충분하다. 매번 같은 강도로 반복 검증하지 않는다.
4. 가장 단순한 동작을 먼저 구현한다. 최적화는 측정 후 별도로(premature optimization 금지).
5. **커밋 / `git push`는 사용자의 명시적 요청이 있을 때만 한다 — 절대 자동으로 하지 않는다.**
   다만 의미 있는 커밋 단위가 만들어지면 **먼저 제안한다**: 고민의 과정이 담긴 커밋 히스토리 자체가 산출물이다.
6. 코드 변경 후 리뷰한다: `./gradlew build`(컴파일+테스트) 통과 → cross-cutting(타임아웃 경계, 입력 검증,
   트랜잭션 경계, 실패 처리, 로깅) → 코드 품질(타입·중복·네이밍·단일 책임).
7. 리팩터 전, 회귀를 잡을 테스트가 있는지 확인한다. 얇으면 테스트를 먼저 쓴다.
8. 선행 리팩터는 기능과 분리한다 — refactor → review → feature 순으로 커밋 단위를 나눌 수 있게 작업한다.
9. 리뷰 중 발견한 범위 밖 개선은 묻어두지 않는다. **발견마다 지금 고칠지 / 넘어갈지 판단하고, 이유와 함께
   사용자에게 알린다.** 지금 로드된 컨텍스트는 다음 세션엔 없으니 사소해도 언급 없이 넘어가지 않는다.
10. 기존 경고를 작업 중간에 고치지 않는다(본인 diff가 만든 경고는 커밋 전 수정).
11. **외부 API·프레임워크 기능을 건드리기 전 공식 문서를 확인한다.** Boot 4는 3.x에서 모듈·패키지가
    이동한 지점이 많다. 기억·추측에 의존하지 않는다.
12. **라이브러리 채택은 런타임 동작 근거로만 정당화한다** — "코드가 줄어듦 / DX 좋음"은 근거가 아니다.
    손으로 짠 것과 런타임 동작이 같으면 도입하지 않는다. (**예외: Lombok** — 런타임 의존성이 아니라
    컴파일타임 코드생성기라 이 규칙의 대상이 아니다.)
13. **버그 픽스는 버그 클래스 제거까지 제안한다** — 회귀 테스트·ArchUnit 규칙·타입/제약으로 같은 부류를
    원천 차단할 수 있는지. 픽스가 먼저, 예방은 후속.
14. **추상화 정직성**: 패턴·알고리즘 이름을 빌렸으면 런타임 의미가 그 계약과 실제로 일치해야 한다.
    아니면 실제 동작을 서술하는 정직한 이름으로.
15. **주석은 "왜"만 쓴다.** 설정값의 근거, 검토했다 버린 대안, 보수적 처리를 택한 이유.
    **금지**: 무엇을 하는지 설명하는 주석, Javadoc, 클래스·메서드 요약 주석. 코드를 읽으면 알 수 있는 것을
    반복하지 않는다. 타임아웃 값·재고 판정 규칙처럼 **"왜 이 숫자·이 규칙인가"가 코드에 남아야 하는 것**이 대상이다.
16. **완료 판정은 "돌아가는 동작"으로 한다.** 컴파일 통과 ≠ 완료. 견고성(타임아웃·부분 실패)은 "구현했다"가
    아니라 **Mock 모드를 바꿔가며 도는 테스트**가 근거다.
17. **설계 판단이 바뀌면 같은 세션에 문서를 갱신한다**(`docs/`·`README.md`·이 파일).
    코드가 문서와 모순된 채 방치되면 완료가 아니다.
18. **구현 중 사고를 짧게 브리핑한다** — 무엇을·왜 바꾸는지, 어떤 트레이드오프를 택하는지 — 사용자가
    중간에 교정할 수 있도록.
19. **설명할 수 없는 코드를 저장소에 남기지 않는다.** AI가 생성한 코드는 전량 정독하고, 납득되지 않으면
    고치거나 버린다. 임의 지점을 짚어 근거를 설명할 수 있어야 한다.
20. **JOURNAL 기록은 사후에 몰아 쓰지 않는다.** 판단이 뒤집힌 순간, 막힌 지점, AI에게 무엇을 물었고 그 답을
    어떻게 수용·수정·거부했는지를 **그때** 남긴다 — 나중에는 재구성되지 않는다.

## Priorities (시간이 부족할 때)

**버리는 순서**: 재시도·서킷 브레이커 → 청크 분할 → 관측성.
버린 것은 구현 대신 **설계 문서로 남긴다**(무엇을·왜 안 했는지가 근거가 된다).

**버리지 않는 것**: 견고성 통합 테스트와 문서.
이 둘이 없으면 나머지를 만들었다는 근거 자체가 사라진다.

핵심 흐름(Mock → 카탈로그 동기화 → 어댑터 → 통합 검색 → 부분 실패)이 끊기면
나머지는 의미가 없다. 여기를 먼저 관통시킨다.

**어댑터를 만들 때 `SupplierResult`를 같이 만든다.** 나중에 붙이면 구조가 꼬인다.

Mock은 **코드 품질을 보지 않는다.** 시간을 쓰지 않는다.

---

## Rules (경로별 세부 규칙)

계층·모듈별 세부 규칙은 `.claude/rules/`에 나눠 두었다.
각 파일은 frontmatter의 `paths`에 걸린 파일을 다룰 때 함께 읽힌다.
**규칙이 바뀌면 해당 파일을 같은 세션에 갱신한다**(규칙 17).

| 파일 | 적용 대상 | 담은 것 |
|---|---|---|
| `entity.md` | 엔티티 · `catalog` | Lombok 제약, **유니크 제약 선언**, soft delete |
| `repository.md` | `*Repository` | JPA 접근 경계, upsert 멱등성, N+1 회피 |
| `service.md` | `*Service` | 트랜잭션 경계, **리액티브 체인에서 JPA 금지**, 병합·지표 |
| `controller.md` | `*Controller` | **검색 API 계약**, 요청 검증, 응답 상태, `.block()` 위치 |
| `dto.md` | DTO · `*Request` · `*Response` | record 기본, **Jackson 3 함정**, 요금 2계층, 응답 필수 항목 |
| `adapter.md` | `supplier..` | **어댑터 경계**, 실패를 값으로, 타임아웃, 정규화 규칙 |
| `testing.md` | 테스트 | Testcontainers, **Boot 4 슬라이스 패키지**, 무엇을 어느 층에서 검증하나 |
| `mock-supplier.md` | `:mock-supplier` | 3가지 모드, **장애 표현을 공급사마다 다르게** |
| `commit.md` | 전체 (항상) | **커밋 메시지 작성 규칙**, 분리 기준, 히스토리 rewrite 금지 |

여기서 옮겨간 내용은 이 파일에 중복해 두지 않는다. 한쪽만 고치면 두 문서가 어긋난다.

---

## Documents

- `README.md` — 빌드·실행 방법과 주요 설계 의사결정. **여기 적은 내용이 실제 코드에 있어야 한다.**
  최소한 아래 판단의 근거를 담는다:
  표준 요금 모델에서 무엇을 취하고 무엇을 버렸는지 / 예약 불가 상품을 노출한 이유 / 타임아웃 값의 근거 /
  매핑 동기화 시점 / MVC + WebClient 조합을 택한 이유 / 신규 공급사 추가 시 고칠 것 /
  숙소가 수천 개로 늘 때의 대응 / 구현하지 않은 것과 그 이유.
- `docs/` — 요구사항 해석, 아키텍처, 통합 모델, API 명세, 구현하지 않은 항목의 설계.
  **외부 원문 자료는 여기에도 두지 않는다**(Repository policy).
- `JOURNAL.md` — 일자별 진행, 뒤집은 판단과 이유, 막힌 지점, 포기한 것과 이유, AI 활용 기록(규칙 20).

## Language policy

- **코드 식별자·패키지명·테스트 메서드명: 영어.**
- **주석·문서(README·docs·JOURNAL)·커밋 메시지: 한국어.**
  이 저장소의 산출물은 설계 근거를 한국어로 설명하는 것이다.

## Commit convention

**PR을 쓰지 않는다. 커밋 히스토리가 유일한 서사다.** 결과물만이 아니라 고민의 과정이 읽혀야 한다.

- conventional prefix(영어) + **한국어 제목**. 제목에 **판단 근거**를 담는다 —
  무엇을 바꿨는지가 아니라 **왜 그렇게 정했는지**:
  `feat: 연박 재고를 기간 내 최솟값으로 판정 — 누락 날짜는 0 처리(과판매 방지)`
- **하나의 커밋 = 하나의 판단.**
- **push 이후에는 히스토리를 다시 쓰지 않는다.** 판단이 뒤집혔으면 되돌리는 새 커밋을 쌓는다.
- **AI 트레일러·세션 링크 금지**(Repository policy).

전체 규칙은 `.claude/rules/commit.md`(prefix 목록, 본문에 담을 것, 커밋 분리 기준,
히스토리 rewrite 예외, 커밋 전 확인 5가지).

## Local development

- 전제: **Docker Desktop 실행 중.** PostgreSQL은 Spring Boot Docker Compose Support가 `bootRun` 시
  자동 기동하고(`compose.yaml`은 저장소 루트), 통합 테스트는 Testcontainers가 별도로 띄운다.
- 본체: `./gradlew :app:bootRun` (8080) · Mock 공급사: `./gradlew :mock-supplier:bootRun` (9090)
- **둘 다 띄워야 검색이 동작한다.** Mock 서버 없이 검색을 호출하면 전 공급사 연결 실패가 되고,
  이는 정상 동작(`ALL_FAILED`)이지 버그가 아니다.
- 빌드·테스트: `./gradlew build`
