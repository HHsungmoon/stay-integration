# 개발 기록

의사결정 과정, 뒤집은 판단, 막힌 지점, 포기한 것과 이유, AI 활용 기록.
완성된 결과보다 **어떻게 그 결론에 도달했는지**를 남긴다. 단계별 설계·대안·구현하면서 달라진 것·미구현 항목은 `docs/01`~`09`에 있고,
여기는 **시간순으로 판단이 어떻게 바뀌었나**와 **AI를 어떻게 썼나**만 적는다. 날짜가 1단계, 문서 번호가 2단계다.

## 한눈에

| 날짜 | 문서 | 단계 | 핵심 판단 한 줄 | 뒤집은 것 |
|---|---|---|---|---|
| 09-02 | 01 | 요구사항 해석 | 판단 13건을 코드 전에 확정. 요금은 2계층, 타임아웃은 3계층 | 요금 모델 "공통 분모만" → 2계층 |
| 09-03 | 02 | 기술 선정·환경 | Boot 4.1.1 + Java 25, MVC + WebClient, Testcontainers 실제 PostgreSQL, Mock은 별도 모듈 | Flyway 도입 → 철회, H2 → Testcontainers |
| 09-05 | 03 | Mock 공급사 | 장애 표현을 공급사마다 다르게, 무응답은 `DeferredResult`, 데이터가 곧 시나리오 | `Thread.sleep` → `DeferredResult` |
| 09-05 | 04 | 카탈로그 | 유니크 제약이 스키마의 유일한 선언 지점, 사라진 상품은 비활성화(ID 보존), `function` 계층 | 서비스의 repository 직접 접근 → function |
| 09-05 | 05 | 어댑터 | `FailureKind` 8값 + `retryable()`, `exchangeToMono`, 호출 골격 `SupplierCallPipeline`, 항목 결함은 `rejected` | 3분류 → 8값, `starter-webflux` → `starter-webclient`, MockWebServer → `ExchangeFunction` 스텁 |
| 09-05 | 06 | 검색·부분 실패 | 청크 50·동시 4·데드라인 3s(도착분 유지), 병합은 순수 함수, ArchUnit | D-9 "설계로" → 구현, `@ModelAttribute` → `@RequestParam` + 생성자 검증 |
| 09-05 | 07 | 서킷 브레이커 | 공급사당 하나, 파이프라인 맨 바깥, `recordResult` = `retryable()` | 문서의 라이브러리 동작 서술 3곳 → 바이트코드로 뒤집음 |
| 09-05 | 08 | 관측성 | 지표는 병합 지점에서 `SupplierResult`를 한 번 더 보는 것으로 | `outcome` 5값 → 9값 |
| 09-06 | 09 | 예약 대행(설계) | 쓰기는 "됐는지 모른다"가 생긴다 — 상태 머신 + 조정, 재시도는 여기서 옳다 | README "범위 밖" → 선택 항목·설계 |
| 09-06 | — | 점검·마감 | 규모 와이어 테스트, 컨테이너, 구조화 로깅, springdoc, 문서 전수 대조 | "청크 경로 검증됨" → 실제로는 안 돌렸음 |

---

## 2026-09-02 — 요구사항 해석 (`docs/01`)

시간을 많이 쓰지 못한 날이다. 요구사항을 읽고 판단이 갈리는 지점 13건을 코드 전에 확정하는 데 집중했다(`01` §3.10).

가장 오래 고민한 것은 **요금 모델**이었다. 한쪽은 날짜별 단가, 다른 쪽은 총액만 준다. 총액에서 날짜별 단가는 만들 수 없다. 처음엔 "공통 분모만 남기고 날짜별 내역을
버린다"였다가 뒤집었다 — 어댑터가 이미 파싱한 값이라 보존 비용이 0인데 버리면 영구히 잃는다. 필수 계층 + nullable 확장 계층으로 나누고 "의사결정 로직은 필수
계층만으로 가능해야 한다"를 계약으로 못 박았다(`01` §3.2.2). 타임아웃은 "연결 + 응답" 2계층 제안을 받았는데 청크가 여러 개면 개별 타임아웃의 합이 목표를 넘는다는
문제가 보여 **오케스트레이션 데드라인을 3번째 계층**으로 넣었다(`01` §3.7). 과거 체크인은 거부하지 않기로 했다 — 막으면 "오늘"에 따라 같은 요청의 성패가 갈려
테스트·문서 예시가 시간이 지나면 깨진다.

---

## 2026-09-03 — 기술 선정과 환경 (`docs/02`)

이날도 길게 쓰지 못했다. 기술 후보를 등급(★·○·△)으로 정리하고(`02`), 멀티모듈 전환과 테스트 DB 구성을 했고, `CLAUDE.md`를 썼다.

`CLAUDE.md`는 개인 프로젝트에서 쓰던 컨벤션 규칙(레이어·주석·커밋·DTO 규칙)을 가져와 이 프로젝트에 맞게 다듬은 것이다. 이유는 시간이다 — AI가 만든 코드를
전량 읽어야 하는데, 내가 익숙한 스타일로 나오면 읽는 비용이 가장 작다. 규칙을 먼저 주지 않으면 매 산출물마다 스타일을 고치느라 시간을 쓰게 된다.
(경로별 세부 규칙 `.claude/rules/`로 나눈 것과 커밋 정책 훅은 9/5 첫 작업이다.)

뒤집은 판단이 셋이다. **Flyway** — 도입하려 했다가 철회. "운영에서 `ddl-auto=update`를 쓰지 않는다"는 일반론이 근거였는데, 테이블 2개 규모에서 마이그레이션 이력은
이 저장소의 산출물이 아니다. 불변 조건("같은 공급사 상품은 항상 같은 내부 ID")의 강제 지점을 엔티티의 유니크 제약 선언에 두고, 그게 실제 DB에 생기는지를 테스트가 보게 했다.
**H2** — 빠르고 Docker도 필요 없지만, 매핑의 upsert 멱등성과 유니크 제약이 불변 조건인데 방언이 다른 DB로는 "테스트는 통과하는데 운영에서 깨지는" 상황을 못 잡는다.
Testcontainers 실제 PostgreSQL로. **요구사항 분석 문서를 "확정본"으로 커밋했다가 철회** — 다시 읽으니 바뀔 내용이 보였다. 이미 push한 뒤라 히스토리를 다시 썼고,
이후 `docs/`를 확정 전까지 추적에서 뺐다. push 뒤 히스토리 재작성은 이때 한 번이고, 규칙으로 금지했다.

**Mock을 별도 Gradle 모듈로** 둔 것도 이날이다. 같은 프로세스면 본체가 자기 자신을 HTTP로 부르고, 무응답을 재현했을 때 그게 공급사 지연인지 톰캣 스레드 고갈인지
구분할 수 없다. **실패를 sealed interface로** 표현하기로 했다 — 나중에 "호출조차 하지 않음"(서킷)이 추가될 때 그 타입을 다루는 switch가 전부 컴파일 에러로 드러난다.
실제로 케이스를 지우고 붙여 컴파일 에러가 나는지 확인한 뒤 채택했다.

막힌 지점: `contextLoads`가 실패했는데 `spring-boot-docker-compose`가 `developmentOnly`라 **테스트 클래스패스에 없다**는 것이 원인이었다. 멀티모듈 전환 직후라
내가 깨뜨린 줄 알았는데 이전 커밋을 꺼내 돌려보니 전환 전부터 있던 문제였다. Testcontainers 2.x는 좌표·클래스 위치가 1.x와 달라 두 번 막혔다 — 최신 스택을 고른
대가를 여기서 처음 치렀다.

---

## 2026-09-05 — Mock부터 관측성까지, 핵심 흐름을 하루에 관통 (`docs/03`~`08`)

하루에 33커밋. 먼저 개발 지침을 `CLAUDE.md` 한 덩어리에서 `.claude/rules/*.md`(경로별)로 나누고 커밋 전 저장소 정책(무시 파일·금지어·AI 트레일러) 검사 훅을 달았다.
그 뒤 단계마다 설계 문서 → A/B 결정 → 구현 → 같은 문서 하단에 달라진 것·막힌 지점 기록의 리듬으로 갔다. 상세는 각 문서의 §14·§15에 있고 여기는 판단이 바뀐 것만 적는다.

**03 Mock** — 무응답을 `Thread.sleep`이 아니라 완료시키지 않는 `DeferredResult`로. 스레드를 점유하면 Mock 풀이 말라붙고 그때 원인이 본체인지 Mock인지 구분이 안 된다(`03` §2.3).
장애 표현을 공급사마다 다르게(A는 503, B는 200 + `E503`) 두어 "실패 판정 통일"을 증명할 대상을 만들었다. 데이터는 스펙 예시 그대로 두고 각 행이 하나씩 검증하게 골랐다(`03` §3) —
이 선택이 이틀 뒤 "예시만으로 테스트를 세웠다"는 구멍으로 돌아온다.

**04 카탈로그** — `function` 계층 도입. 서비스가 repository를 직접 잡으면 서비스끼리 서로를 주입하게 되고 그게 순환 참조의 시작이다(`04` §13). 사라진 상품은 삭제하지 않고
`active=false`로 — 재등장 시 같은 ID여야 멱등성이 산다. 3회 반복·재등장 테스트로 증명했다(`04` §4.1).

**05 어댑터** — `FailureKind`를 재시도 여부 3분류에서 **원인 8값 + `retryable()`**로. 로그와 지표에서 "왜 실패했나"를 읽을 수 없었다(`05` §3). 이 결정이 서킷의 기록 기준(07)과
지표 태그(08)에서 두 번 더 값을 했다. `starter-webflux` → `starter-webclient`(Boot 4가 WebClient 자동설정을 별도 모듈로 쪼갰다, `05` §6.1). MockWebServer 대신 Spring
`ExchangeFunction` 스텁, 타임아웃·연결 거부는 Mock을 **같은 JVM의 두 번째 Boot 컨텍스트**로 띄운 와이어 테스트가 맡는다. 막힌 지점 넷은 `05` §15 — 두 번째 컨텍스트가
본체 yaml을 읽어 8080을 물려던 것이 가장 오래 걸렸다.

**06 검색** — D-9 "데드라인 초과 시 완료분만 응답은 설계로"를 **구현**으로 바꿨다(B). 같은 D-9가 청크 단위 부분 실패를 허용하는데 데드라인만 다르게 취급하는 건 일관되지
않고 비용이 `take(deadline)` 한 줄이었다. 핵심은 청크 수 = 결과 수 보정 — 없으면 `failedCalls`가 거짓이 된다(`06` §4). 요청 바인딩은 `@ModelAttribute` record에서 `@RequestParam`
넷 + 생성자 검증으로 — record 바인딩은 누락과 형식 오류를 나눌 수 없었다(`06` §14). ArchUnit을 검색 코드 **전에** 두었고, 어댑터 코드가 이미 경계를 지키고 있음이 첫 실행에서 확인됐다.

**07 서킷** — 이틀 전 "대충 만든 서킷은 없느니만 못하다"로 미뤘던 항목을 확정으로 올렸다. 순서를 견고성 테스트 뒤로 둔 이유는 `Skipped`를 다루는 코드가 먼저 있어야 서킷이
들어올 때 컴파일 에러로 드러나기 때문 — sealed를 택한 이유가 여기서 작동했다. **설계 문서의 라이브러리 동작 서술 세 곳이 틀렸고 바이트코드로 뒤집었다**(`07` §14). 진짜 함정은
오퍼레이터가 아니라 기본 설정이었다 — 값으로 오는 실패를 `recordResult` 술어 없이 붙이면 서킷이 영원히 열리지 않는다. 재검토에서 `skippedCalls` 구멍도 나왔다(`07` §5).

**08 관측성** — 지표는 병합 지점에서 `SupplierResult`를 한 번 더 보는 것. `outcome` 태그를 5값에서 kind 그대로 9값으로(`08` §2). 가장 큰 불확실성은 "테스트에서 지표가
기록되는가"였다 — Boot 3의 경험(`@SpringBootTest`가 export를 끈다)이 4에서는 틀렸다. jar 목록을 뒤져 확인한 뒤 코드를 썼다(`08` §13).

---

## 2026-09-06 — 점검과 마감 (`docs/09`, 규모·컨테이너·로깅·springdoc·문서 전수 대조)

| 무엇 | 결과 |
|---|---|
| 컨테이너화 — Dockerfile 2개, `compose.yaml` `stack` 프로파일 | `docker compose --profile stack up --build`로 검색·부분 실패·지표까지 확인 |
| 공개 전 점검 — 클린 클론 빌드, 히스토리에 외부 자료 없음, `RestTemplate` 없음, 금지어 0건 | 통과 |
| **규모 테스트** — Mock에 합성 숙소, 와이어 테스트 2개 | 아래 표 |
| 구조화 로깅 — 컨테이너만 ECS JSON, 로그에 `supplier`·`kind` key-value | 실제 컨테이너 로그에서 필드 확인 |
| springdoc 3.1.0 — 요구사항의 권장 항목, 코드 변경 0 | `/v3/api-docs`·Swagger UI, 계약을 테스트로 고정 |
| `RUN.md`, `docs/09`, 01~09 **전수 대조** | 문서마다 "미구현 항목" |

`./gradlew build` 199개(app 168 · mock 31), 컴파일 경고 0.

**인정한 것 셋.** ① **청크 경로를 실제로 돌린 적이 없었다.** README가 "2,000숙소 = 40청크…" 산술을 적어 놓고 예시 3숙소로만 테스트했다. 요구사항 기준으로는 설계로 충분했지만
"완료 판정은 도는 테스트가 근거"라는 자기 규칙 위반이었다. Mock에 합성 숙소를 붙였는데, 모드와 데이터를 처음에 분리해 둔 덕에 바뀐 건 조회 인자 하나와 제어 엔드포인트 하나였다(`03` §10).
② **예약 대행을 "범위 밖"에 넣은 것은 틀렸다.** 요구사항의 선택 항목이라 `docs/09`에 설계를 썼다. ③ **springdoc·구조화 로깅은 결정이 아니라 누락이었다.** 02에 ○·★로 적고 도달하지
못했다. 둘 다 이날 넣었다.

| 규모 테스트 | 결과 |
|---|---|
| 공급사당 122·121코드, 정상 | 청크 3·3, `OK`, items 246, **57ms** |
| 300·299코드, A 지연 1s, 데드라인 1.5s | 청크 6, 동시 4 → 파동 2. 첫 파동 4청크 `Success`, 둘째 2청크 `TIMEOUT`. `PARTIAL`, **1,516ms** |

둘째 줄이 06의 B("도착한 청크는 살린다")가 와이어에서 도는 첫 증명이다.

**문서 전수 대조.** 03~09를 코드 기준으로 다시 읽었더니 06의 `@ModelAttribute`·`validate()`, 04의 리포트 JSON 형태, 03·05의 MockWebServer 언급, 07의 JSON 필드 누락 등이
어긋나 있었다. 각 문서 §14에는 적혀 있었지만 **설계 절 본문은 구현 전 문장 그대로**였다 — 여러 단계를 하루에 몰아 지나가며 "같은 세션에 문서 갱신" 규칙이 무너진 지점이다.
본문을 구현 기준으로 고치고 문서마다 "미구현 항목"을 달았다. 01·02도 같은 방식으로 대조했다.

---

## 고려했지만 하지 않은 것 — 전체

각 문서의 "미구현 항목" 절에 이유가 있다. 여기는 **처음 어디서 고려했고 최종적으로 어떻게 됐는지**만 모은다.

| 항목 | 처음 고려 | 최종 | 이유 한 줄 | 상세 |
|---|---|---|---|---|
| 재시도(`retryWhen`) | 01 §3.7, 02 §2 | **안 함** | 응답 2s × 2회 = 4s > 데드라인 3s. 서킷이 "아는 실패에 시간을 쓰지 않는" 쪽을 맡았다 | 05 §13 |
| 서킷 브레이커 | 02 §2(조건부) | **구현** — 미뤘다가 확정 | Boot 4 호환 확인. 견고성 테스트 뒤에 넣어야 `Skipped`가 컴파일 에러로 드러난다 | 07 |
| Flyway | 02 §6(도입 예정) | **철회** | 테이블 2개에서 마이그레이션 이력이 산출물이 아니다 | 9/3, 04 |
| H2 | 02 §5 | **철회 → Testcontainers** | 방언이 다른 DB로는 유니크 제약·멱등성을 못 잡는다 | 9/3 |
| MockWebServer / WireMock | 02 §5 | **안 함 → `ExchangeFunction` 스텁** | BOM 밖, 최신이 alpha/beta. Spring 스텁이 같은 일을 의존성 없이 한다 | 05 §10 |
| 구조화 로깅 | 02 §3(★) | **구현** — 누락됐다가 9/6 | 컨테이너만 JSON. 수집기 없는 콘솔은 텍스트 유지 | 02 §12, 08 |
| springdoc | 02 §7(○) | **구현** — 누락됐다가 9/6 | 요구사항의 권장 항목. 3.x가 Boot 4와 해결됨, 코드 변경 0 | README |
| Micrometer Tracing | 02 §3(○) | **안 함** | 리액티브 컨텍스트 전파가 별도 주제. 지표가 먼저 | 08 |
| Prometheus + Grafana | 02 §3(○) | **안 함** | 스크레이퍼가 없다. 붙일 때는 설정 두 줄 | 08 §6 |
| GitHub Actions | 02 §8(★) | **미구현** | 마감 단계까지 도달하지 못했다. 클린 클론 빌드는 로컬로 확인 | 02 |
| Version Catalog · Awaitility · JaCoCo · Spotless | 02(○) | **안 함** | 런타임 동작이 달라지지 않는다 | 02 §10 |
| MapStruct | 02 §9 | **안 함** | 응답 → 표준 모델 변환이 핵심 판단이라 자동 생성에 맡기면 설명할 부분이 사라진다 | 02 §9 |
| Redis 캐시 / 결과 캐시 | 02 §9, 01 선택 항목 | **안 함** | 총액이 검색 조건 종속값, 재고는 호출마다 바뀐다. 다음 후보는 요청 합치기 | 06 |
| Kafka / 이벤트 | 02 §9 | **안 함** | 동기 검색 1건의 흐름에 자리가 없다 | 02 §9 |
| WebFlux 전면 도입 | 02 §9 | **안 함** | 검색 외 흐름은 JPA. WebClient만 쓴다 | README |
| 중복 상품 병합 | 01 선택 항목 | **안 함** | 공통 키가 없다. 잘못 합치면 다른 숙소를 하나로 보여준다 | README |
| 통화 환산 | 01 선택 항목 | **안 함** | 정책 문제. 현재 데이터는 KRW 하나 | 05 |
| 예약 대행 | 01 선택 항목 | **설계만** | 공급사 스펙에 예약 API 계약이 없다 | 09 |
| 주기 동기화 스케줄 | 01 D-8 | **안 함** | 관리 엔드포인트가 대신한다 | 04 |
| API당 서킷 · 다중 인스턴스 상태 공유 | 07 | **안 함** | 카탈로그는 자기 창을 못 채운다. 단일 인스턴스 가정 | 07 |
| `includeSoldOut` · `priceDetail` 파라미터 · 정렬·페이징·지역 필터 | 01 비범위/06 | **안 함** | 범위 밖 또는 지금 규모에서 필요가 드러나지 않음 | 06 |

---

## AI 활용

**어떻게 나눠 일했나.** AI(Claude Code)에게는 설계 문서 초안, 구현, 테스트 작성, 문서와 코드의 대조를 맡겼다. 내가 맡은 것은 요구사항 해석의 최종 판단, 우선순위,
단계마다 갈리는 결정, 그리고 산출물을 읽고 되묻는 일이다. 방식은 단계마다 같았다 — 설계 문서를 먼저 쓰게 하고, 문서 안에서 결정이 갈리는 지점은 선택지와 비용을 놓고
내가 고르고(데드라인 처리 A/B, 지표 태그 A/B가 그렇게 정해졌다), 구현이 끝나면 같은 문서 하단에 달라진 것과 막힌 지점을 적게 했다. 커밋은 내가 요청할 때만 만들고
단위도 내가 정했다. 커밋 히스토리가 산출물이기 때문이다.

**규칙을 먼저 줬다.** `CLAUDE.md`와 `.claude/rules/`는 개인 프로젝트에서 쓰던 컨벤션을 이 프로젝트에 맞게 다듬은 것이다. 시간이 가장 적은 자원이었고, AI가 만든 코드를
전량 읽어야 하는 상황에서 낯선 스타일을 읽는 비용이 가장 컸다. 주석은 "왜"만, 레이어 서브패키지, 축약 없는 변수명, 라이브러리 채택은 런타임 근거로만, 설명할 수 없는 코드는
남기지 않는다 — 이런 규칙이 있어서 산출물이 처음부터 내가 읽을 수 있는 형태로 나왔고, 규칙에서 어긋난 것(Javadoc, 임의 커밋)은 규칙을 고쳐 다음부터 막았다.

**AI가 잘한 것.** 넓게 훑는 일이다. 설계 대안을 빠짐없이 나열하고, 테스트 케이스를 상태 표 전체에 대해 만들고, 문서 열 개를 코드와 대조해 어긋난 문장을 찾는 일은 사람이 하면
빠뜨린다. 그리고 **검증을 시키면 실제로 한다** — 라이브러리 jar를 받아 바이트코드를 읽었고, 떠 있는 컨테이너의 지표 값을 조회했고, 테스트 결과 파일의 숫자로 문서를 대조했다.
Boot 4처럼 경험이 얇은 스택에서 이게 값을 했다. `WebClient.Builder` 자동설정 모듈 분리, 테스트에서 지표 export가 꺼지지 않는 이유, 반열림 판정 시점 — 셋 다 확인해서 잡았다.

**내가 있어야 했던 곳.** 방향을 정하는 일과, 그럴듯한 문장을 의심하는 일이다.
- 서킷 설계 문서에 "어댑터가 실패를 값으로 돌려주니 `CircuitBreakerOperator`를 쓸 수 없다"고 적혀 있었다. 근거 없이 단정하는 문장이라 되물었고, 확인 결과 반대였다 —
  오퍼레이터는 값을 볼 수 있고 진짜 함정은 기본 설정이었다. 같은 문서에서 두 곳이 더 틀렸다. 이때부터 **라이브러리 동작 서술은 검증 전에는 가설**로 취급하고, 설계 문서에
  "구현 전 확인" 표를 두게 했다.
- 공개 전 점검에서 "Mock 데이터가 스펙 예시와 같으니 우리 값으로 바꾸자"는 A/B가 올라왔다. 문제는 값이 아니라 **양**이었다 — 예시 3숙소로는 50개 청크 경로가 한 번도 돌지 않는다.
  질문을 다시 던지자 규모 테스트가 나왔다.
- 예약 대행이 요구사항의 선택 항목인데 README에 "범위 밖"으로 분류되어 있었다. 내가 요구사항을 다시 확인해 잡았다.
- 문서 전수 대조는 "추론·추측 금지, 지금 구현된 코드 기준으로"라는 조건을 걸었을 때만 제대로 됐다. 조건 없이 맡기면 문서를 문서로 고친다.
- 요청하지 않은 검증(커밋마다 worktree 컴파일)과 결과물을 채팅에 쏟는 일은 끊었다. 토큰과 시간이 유한하고, 파일에 쓰게 하고 내가 파일을 읽는 편이 빨랐다.

**무엇을 물었고, 답을 어떻게 다뤘나 — 판단이 갈린 지점만.** 스타일 교정이나 한 줄짜리 수정은 뺐다. 상세는 오른쪽 문서에 있다.

| 단계 | AI가 낸 것 | 판정 | 왜 | 근거 |
|---|---|---|---|---|
| 01 요금 모델 | 공통 분모(총액·gross)만 남기고 날짜별 내역은 버리는 초안 | **수정** → 2계층 | 어댑터가 이미 파싱한 값이라 보존 비용이 0인데 버리면 영구히 잃는다 | `01` §3.2.2 |
| 01 타임아웃 | 연결·응답 2계층 | **수정** → 오케스트레이션 데드라인 추가 | 청크가 여럿이면 개별 타임아웃의 합이 3초를 넘는다. 3번째 계층이 없으면 값의 근거가 완결되지 않는다 | `01` §3.7 |
| 02 WebClient 의존성 | `starter-webflux` | **수정** → `starter-webclient` | Boot 4는 WebClient 자동설정을 별도 모듈로 뺐다. 의존성 트리로 확인 — `starter-webflux`만으로는 `WebClient.Builder` 빈이 없다 | `05` §6.1 |
| 05 실패 분류 | 재시도 여부 3분류 | **수정** → 원인 8값 + `retryable()` | 로그·지표에서 "왜 실패했나"를 읽을 수 없었다. 같은 술어가 서킷 기록 기준과 지표 태그가 됐다 | `05` §3 |
| 06 요청 바인딩 | `@ModelAttribute` record + Bean Validation | **수정** → `@RequestParam` 넷 + 생성자 검증 | 누락과 형식 오류가 필드 오류 하나로 뭉개져 400의 `code`를 나눌 수 없었다 | `06` §14 |
| 06 데드라인 초과 | A(설계로만) / B(도착한 청크는 살린다) 선택지 | **B 선택** | 청크 단위 부분 실패 허용과 일관. 비용은 `take(deadline)` 한 줄 + 청크 수 보정 | `06` §4 |
| 07 서킷 | "어댑터가 실패를 값으로 주니 `CircuitBreakerOperator`를 쓸 수 없다" | **거부** — 바이트코드로 확인, 반대였다 | 오퍼레이터는 방출 값을 본다. 진짜 함정은 `recordResult` 기본값 — 없으면 서킷이 영원히 열리지 않는다 | `07` §1·§14 |
| 07 반열림 | "반열림에서 1회 실패면 다시 열린다" | **거부** — 확인 후 뒤집음 | 허용 호출이 전부 기록된 뒤 실패율로 판정한다. 테스트가 "3건 중 1건 실패는 닫힘"을 고정한다 | `07` §14 |
| 08 `outcome` 태그 | 5값 / kind 그대로 9값 선택지 | **9값 선택** | 429와 5xx가 합쳐지면 동시성 상한의 근거(`RATE_LIMITED` 비율)를 잃는다 | `08` §2 |
| 08 테스트의 지표 | "`@SpringBootTest`는 지표 export를 끈다"(Boot 3 경험) | **거부** — jar 목록으로 확인 | Boot 4는 그 커스터마이저를 별도 모듈로 뺐고 우리는 쓰지 않는다. 와이어 테스트가 `count >= 1`을 단언하는 이유 | `08` §13 |
| 09 범위 | 예약 대행을 "범위 밖"으로 분류 | **거부** | 요구사항의 선택 항목이다. 설계 문서로 답했다 | `09` |
| 마감 Mock 데이터 | "스펙 예시와 같은 값을 우리 값으로 바꾸자" | **거부 후 재질문** | 문제는 값이 아니라 양이었다. 합성 숙소를 붙여 50개 청크 경로를 실제로 돌렸다 | `03` §10 |

**AI를 쓰지 않은 것.** 외부 자료와 회사명을 저장소에서 어떻게 다룰지는 내가 정하고 훅으로 강제했다. 어떤 것을 구현하고 어떤 것을 설계로 남길지도 내가 정했다 — AI는
선택지를 놓았고 우선순위(버리는 순서: 재시도 → 청크 → 관측성)는 첫날 내가 적은 것이다.

**배운 것.** 첫째, AI의 "라이브러리는 이렇게 동작한다"는 문장은 확인하기 전까지 가설이다 — 이번에 네 번 틀렸고 네 번 다 확인해서 잡았다. 둘째, 문서 갱신을 단계 넘기며 미루면
설계 절 본문이 구현 전 문장으로 남는다. 마지막 날 전수 대조가 없었으면 그대로 공개됐을 것이다. 셋째, 예시 데이터로 시작하는 건 맞지만 문서에 적은 산술은 데이터를 늘려 실제로 돌려봐야 한다.

---

## 참고 자료

"이렇게 동작한다"는 문장을 확인할 때 돌아간 곳. 읽고 나서 코드를 썼다.

- Spring Boot 4.0 Migration Guide — 모듈 분리(`spring-boot-webclient`)와 슬라이스 테스트 패키지 이동: https://github.com/spring-projects/spring-boot/wiki/Spring-Boot-4.0-Migration-Guide
- Spring Framework WebClient — `exchangeToMono`, 커넥터 교체: https://docs.spring.io/spring-framework/reference/web/webflux-webclient.html
- Spring Boot Logging — 구조화 로깅(ECS) 설정: https://docs.spring.io/spring-boot/reference/features/logging.html
- Resilience4j CircuitBreaker — 슬라이딩 윈도우·반열림·`recordResult`: https://resilience4j.readme.io/docs/circuitbreaker
- Reactor `Flux` — `take(Duration)`·`flatMap(…, concurrency)`·`timeout`: https://projectreactor.io/docs/core/release/api/reactor/core/publisher/Flux.html
- Testcontainers for Java — 2.x 모듈 좌표: https://java.testcontainers.org/
- ArchUnit User Guide — `layeredArchitecture()`: https://www.archunit.org/userguide/html/000_Index.html
- springdoc-openapi — Boot 4에 맞는 3.x: https://springdoc.org/
